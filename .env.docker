# Configuración para Docker
# Esta configuración se usa cuando se ejecuta en contenedores Docker

# Base de datos (usa el nombre del servicio como host)
DATABASE_URL="postgresql://postgres:password@postgres:5432/dla_db?schema=public"

# Si necesitas otras variables de entorno para producción, agrégalas aquí
# NEXT_PUBLIC_API_URL="https://api.tudominio.com"
